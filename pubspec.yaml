name: hysm_alumni
description: Official HYSM Alumni Management Platform
publish_to: "none"

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  provider: ^6.1.2
  google_fonts: ^6.2.1
  intl: ^0.19.0
  url_launcher: ^6.2.5
  pay_with_paystack: ^1.0.14
  flutter_spinkit: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
